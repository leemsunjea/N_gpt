# filepath: /Users/imsunjae/Documents/GitHub/N_gpt/Dockerfile
FROM python:3.11-slim AS build

# 작업 디렉토리 설정
WORKDIR /app

# 시스템 패키지 업데이트 및 필요한 패키지 설치
RUN apt-get update && apt-get install -y --no-install-recommends \
    gcc \
    g++ \
    libgomp1 \
    && rm -rf /var/lib/apt/lists/*

# 최종 이미지 준비
FROM python:3.11-slim

# 작업 디렉토리 설정
WORKDIR /app

# 필요한 시스템 패키지만 설치
RUN apt-get update && apt-get install -y --no-install-recommends \
    libgomp1 \
    && rm -rf /var/lib/apt/lists/*

# 필수 디렉토리 생성 및 권한 설정
RUN mkdir -p /app/static /app/templates /tmp/static /tmp/templates \
    && chmod -R 777 /tmp/static /tmp/templates

# 필수 파일만 복사 (최소한의 파일만 포함)
COPY main.py chat_service.py document_processor.py lightweight_embedding.py database.py ./
COPY requirements-minimal.txt ./requirements.txt
COPY templates templates/
COPY start.sh ./
RUN chmod +x start.sh

# 필수 패키지만 설치 (최소한의 의존성)
RUN pip install --no-cache-dir fastapi uvicorn python-multipart jinja2 python-dotenv sqlalchemy asyncpg aiosqlite openai numpy PyPDF2 python-docx aiofiles && \
    pip cache purge

# 정적 파일 준비
RUN touch /app/static/favicon.ico

# 포트 노출
EXPOSE 8000

# 환경변수 설정
ENV PYTHONPATH=/app
ENV PYTHONUNBUFFERED=1
ENV PORT=8000
ENV CLOUDTYPE_DEPLOYMENT=1
# 데이터베이스 연결 환경 변수
ENV DB_USER=root
ENV DB_PASSWORD=sunjea
ENV DB_HOST=svc.sel4.cloudtype.app
ENV DB_PORT=30173
ENV DB_NAME=testdb
# 임시 디렉토리 사용 설정
ENV STATIC_DIR=/tmp/static
ENV TEMPLATES_DIR=/tmp/templates

# 애플리케이션 실행
CMD ["./start.sh"]
